name: Run tests

on:
  push:
    branches:
      - test-pipeline
  pull_request:
    branches:
      - test-pipeline

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: almalinux:9
    steps:
      - uses: actions/checkout@v4
      - name: Install Python and dependencies
        run: |
          dnf update -y
          dnf install -y python3-pip
      - name: Setup environment
        run: |
          python3.9 -m pip install .[dev]
      - name: Run pytest
        run: |
          pytest tests/
